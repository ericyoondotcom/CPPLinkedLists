#ifndef SingleLinkedNode_hpp
#define SingleLinkedNode_hpp

#include <stdio.h>
class SingleLinkedNode
{
public:
    SingleLinkedNode(int val);
    int value;
    SingleLinkedNode* next;
private:
    
};
#endif /* SingleLinkedNode_hpp */
